## 自我介绍

## 计划安排

## feed流文件存储是咋样的

本地存储，实际应该用oss等存储方式

## 视频上传策略

mq异步上传,项目中没加视频状态字段,实际应该要加状态字段用于查询

## 同一个用户连续上传10、20个视频咋办

不清楚

单用户限流,redis存储单位时间上传速度,实际上传Mq也对worker进行并发限制，根据userID设置worker

## jwt鉴权流程

登录返回token,前端每次请求携带token,对于需要鉴权的中间件加权限校验,并且还能通过token存储一些用户信息上下文

## token过期时间设置以及过期恢复方式

项目设置30分钟，实际要根据业务决定，并且我这里设置了刷新机制，采用jwt+redis存储token,校验token通过会进行刷新

## redis常用数据结构

string、hash、set、zset、list(没答出来)、bitmap、geo

## 异步存储是用mysql吗

是的本地存储，mysql存文件路径，直接读文件

## 视频发布详情设计

id、user_id、title、description、create_time、update_time、文件路径

项目设置like_count、comment_coutn不设计评论、点赞表，简单设计，但实际要持久化点赞、评论

点赞人和视频、评论写入redis,然后异步更新Mysql

## 短链应用场景

短信,目的为了减少信息字节占用

## 用户点击短链，服务做了哪些处理

查询长短链映射表并返回对应长链,不过我这里用了缓存(没说用布隆过滤器解决缓存穿透和用sngleflight解决缓存击穿)

## 长短链关系关联

采用mysql存储长短链的映射关系

## Golang for语句

for i:=0;i<n;i++ {}

For range 数据结构

## 没对应下标咋跳出去for

我只听到了退出循环,说了用break

## '100'字符转int

让字符-'0' (应该答对每一位字符-'0'，ans=ans*10+ch[0]的）

strconv.ParseInt解析成对应进制数字

## uint64和int64区别

前者是无符号整数，是非负数

## 往set集合添加元素咋操作

我答的是golang没有set，需要用mao模拟set,然后用map的语法，m[key]=struct{}{}

## channel原理

不清楚，只答了知道应用，然后没问了

## linux常用命令

ll、ls、mv、tar、cd、| grep port

## 查看防火墙有没有开放某个端口命令

Firewalk status（乱猜的）

## 文件编辑咋保存

:wq、:q!等三种方式

## 反问业务开发流程

跟产品对接拆解需求，自己写一个小模块,会拿到开发文档去写

然后有人核对，然后合并分支，然后模块打包交给测试测试

## 反问ai使用情况

有ai负责人,后面会基于ai进行编程

## 工作时间

9~12 13.30！18.30，双休，150