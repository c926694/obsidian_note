2026年7月7日 下午 1:44|34分钟 22秒

关键词:
索引、线程、数据库、缓存、服务器、二叉树、异步、主键、数组、鉴权、多进程、密钥、联合索引、数字签名、一致性哈希、删除节点、二叉排序树、进程通讯

文字记录:
说话人 1 00:01 
你好。

说话人 2 00:02 
哎，可以听清吗？

说话人 1 00:03 
听得清，你听得到吗？

说话人 2 00:06 
可以。

说话人 1 00:07 
好的，要开摄像头。好的。

说话人 2 00:10 
不用。

说话人 1 00:11 
不用。

说话人 2 00:13 
然后可以先简单做个自我介绍，去主动去说一个你做过的模块，里面是怎么做的。

说话人 1 00:21 
面试官你好，我叫陈鹏，是二八届，说错了，二八届计算机科学与技术的学生。我在这期间就学习了 Golang 相关的知识。包括一些够基础语法，比如切片、切片map、 channel 这些，这些组件，以及包够外部，比如进框架，以及就 ORM 框架也学过。中间件MySQL、Redis、卡夫卡这些中间件也学习过。包括一些数据常见的数据结构，比如二叉树图，这些我都也学习过。我的项目就是剪辑上的两个项目， boss 上应该有有项目的。在线解决上面的有两个项目，第一个项目是一个 feed 流项目，类似于抖音那种刷短视频那种 feed 流，那种推送流项目。第二个项目是基于构建了一个分布式缓存。

说话人 2 01:30 
可以，就剪一个模块说一下就行，

说话人 1 01:33 
剪一个模块。

说话人 2 01:34 
对，你认为哪个模块你比较，就是了解比较深？

说话人 1 01:40 
我的 feed 流主要的重点就是有三个推荐流，一个是普通的，对，普，我是负责推荐流，就是滑的，推送的，要比普通那个推荐，比如你打开抖音嘛，也给你推视频。我，不过我那个做法比较简单，我那个推荐流是按照时间来排的，一开始我是采用普通分页，就按 limit 取的，但后面发现效率太低，后面改成。设置为，后面又改成了一个根据一个值，一个类似于一个值，可以进行，就解决这种深度分页问题了。查询的时候，只要查大于那个时间的，因为我是按时间来排序的，这个时间是个递增的值，就根据这个递增值就更好的查询了。

说话人 1 02:34 
这是推荐流了，还有个热度流，热版，类似于排行榜的热版。我采用的是，先用 Redis 进行一个统计每一分钟热度嘛，这个热版是，我是统计，假如说统计多少分钟，一个时间段之类的热度，对吧？用 Redis 统计每一分钟内的，查询的话，前端传个数，就查一个时间段，我们就对那个。对。时间段的视频进行合并，取多少条多少条，就类似这样的思路。 feed 流这里一个我认为比较重要的一个模块。

说话人 2 03:15 
了解，那这里面就做过程中的话，有除了刚才说到那个深度分页，还有涉及到哪些，遇到过哪些问题吗？或者是对，哪些问题？

说话人 1 03:26 
问题的话，算是类似于视频的查询吧，视频查询比可能比较慢，后面一开始用了买，就只能是从 MySQL 取，就发现比较慢，后面改用 Redis 了。就 Redis 它用作缓存，这样子查的话就比较更快一点。以及一些异步的异步任务就有些不是必要的，就用 Kafka 消息队列去做异步的操作，就更加增加用户体验性嘛。

说话人 2 04:02 
嗯，了解，那哪些做了异步化操作呢？

说话人 1 04:05 
异步化的话，比如类似于点赞这种。点赞的话我是，卡不卡后面异步化这样子用户。不过我要点赞的话，做的话是用 Redis 来存储的，我就简单做，因为主要主要功能是体现在 feed 流那相关的那里，我用 Redis 点赞之后，后面。先 Kafka 来存储，然后用 Kafka 异步去点，实现这个点赞功能。还有文件上传就也是采用异步的操作， Kafka 异步异步上传文件了，要不然大文件的话可能就比较慢了，你的用户体验不好，就直接上传，你卡后异步上传跟应用体验更好一点。

说话人 2 04:57 
嗯，了解，这里面写到G， GWT 鉴权与 Token 失效控制，这块指什么？

说话人 1 05:05 
就是对。用户登录之后会生成一个 token 嘛，我是基于基于 JWT 生成token。基于 JWTP 生成 token 中，校验，有时时效了，对吧？通过这个来控制。还有一些，对用户退出的话，我是采用 Redis 来存一个key，当，你鉴权这个过程中，会先去查Redis，会查，先看这个 JWT 是不是有效的，再去查 Redis 来进行鉴权，退出，退出的话就直接相当于把这个 Redis 里面那个 token 给去掉了，这样就是实现用户退出了。

说话人 2 05:50 
那觉得你这里为什么会用用 JWK 呢？不直接用一个token？去做呢？

说话人 1 05:57 
其实也可以用，直接用用别的。我是后面，一开始我是用JWT，但是要实现退出功能的话，发现 JWT 不行，因为 JWT 是无状态的，无法实实现那个主主动退出的，对吧？其实后面就直接引入 Redis 了，就是做的过程中发现发现有这个问题，然后引入Redis，也没管也没管那个，其实用这个一个托，直接生成一个 token 作为这个 Redis 的key，也是可以的。

说话人 2 06:30 
嗯，了解。

说话人 2 06:46 
就问你刚刚提到点赞，你是将数据存到 Redis 嘛，这里面的话，怎么保证它的数据不丢失呢？

说话人 1 06:56 
数据不丢失，我是，我查，哦，我是，我查是查Redis，但我那个数据库里面也保存了Redis，也保存了点赞数，只是，点赞的话是加快这个查询吧。

说话人 2 07:12 
了解，可以说一下，比如说用户作画的一个点赞，的一个接口调用，这个接口里面内部的一个处理逻辑是什么样子呢？

说话人 1 07:22 
处理逻辑是先。就对数据库里面的数据进行，先更新数据库，再更新那个Redis，那个类似于缓存的，就是点赞类似于缓存增加，相当于就是缓存了。所以 Kafka 异步更新Redis，就相当于将 MySQL 和这个 Redis 这个进行解耦了，差不多。你查的话，但不过这个点赞的是有过期时间的，你的相当于两个的一，要考虑一致性问题了，我就考虑用卡，就用 Kafka 来保证，这异步点赞的。

说话人 2 08:09 
那这里为什么不直接去更新 Redis 呢？而去引入一个Kafka？

说话人 1 08:16 
因为可能，直接更新 Redis 可能会有旧的数据，可能读到旧的并发环境下可能会读到旧值啊，引入卡夫卡，异步机制，保证最终一致性嘛。

说话人 2 08:34 
读到旧值是指什么意思啊？

说话人 1 08:38 
你跟，因为你跟数据库和，你可以理解为 Redis 就是那个缓存了，你跟数据库数据库是肯定是主要的了。你更新数据库之后肯定要，你读的话可能读缓存，对吧？你要保证之间的一致性问题。你高并发环境下，可能你更新的数据库，对吧？更新。数据库，但是你要，更新数据库之后，我想想，想想怎么病化，病化环境怎么得出来的？

说话人 1 09:22 
更新数据库之后，就是更新数据库之后是进行三缓的操作，再次读的时候去数据库读，更新，添加到 Redis 缓存中。

说话人 2 09:41 
嗯，了解，那是不是删不去更新了？就比如说，你的点赞数，为什么不直接更新呢？而去，为什么要去删除呢？

说话人 1 09:54 
因为要以数据库这个好像是要以数据库为，点吧，好像。好，这方面不是很，不是很清楚，我记得不是很清楚。

说话人 2 10:12 
哎，好，了解，没关系。

说话人 1 10:14 
嗯，不好意思。

说话人 2 10:16 
那没事。可以说一下这里面，我看你第二个分布式小缓存里面写了一致性哈希，一致性哈希可以说一下吗？就是什么是一致性哈希？

说话人 1 10:28 
一致性哈希的话就是。对于 key 的，对于多实例缓存节点的分配问题。如果假设有多台机器嘛，一台机器是管理一个缓存，对吧？那个 key 传出来key，然后给value。你如果是普通的话，普通哈希就是直接对取模，对一个 key 进行取哈希之后，进行对一个机器数量进行取模，得到分配到哪个哪台机器上，对吧？但是这会导致的原因就是，你如果增加机器之后，就导致有数据失效了，类似于这种，就是你分布不。分布不均导致数据失效的情况。你一次性哈希的话，就是搞一个环，就是一个哈希环吧，你对这个环哈希之后，去进行确定那个机器的位置。

说话人 2 11:38 
他比如说增加节点或删除节点会发生什么？

说话人 1 11:42 
增加节点，删除节点就会。增加节点，删除节点，就是原本有。就会使得key，就假设原本有一台机器上是有缓存的，对吧？但是你增加节点上了之后，使得你那个查找的节点位置错了，就打到另一台机器上了，另一台机器就没有缓存，这样你需要重新建立缓存，对吧？但我一次性哈希的话，就是，确定这个节点位置，会通过虚拟节点，使得这个数据分布更均匀。

说话人 2 12:29 
嗯，了解。

说话人 2 12:50 
平时的话有用过哪些数据库啊？

说话人 1 12:52 
我用平时用过哪些数据库？

说话人 2 12:55 
对。

说话人 1 12:56 
MySQL，Redis， PGSQL 也用过，就跟关系型数据库差不多。

说话人 2 13:04 
了解。还有。那像 MySQL 里面。你说。

说话人 1 13:07 
嗯，你说。

说话人 2 13:09 
那就是刚才像你刚提到MySQL，那 MySQL 里面有用过哪些索引？会了解过哪些索引？

说话人 1 13:15 
索引的话就按那些分类来，比如说主键索引，唯一索引，就普通索引，这些，按按这个分类的话。

说话人 2 13:28 
了解，那项目。唯一索引的话，一般会怎么用的？

说话人 1 13:35 
唯一索引就是唯一标识一个字段的，比如说用户名这些，唯一，就是你那个字段，你能够，你在设计的时候能够保证它的值是唯一的。你索引是增加速度的，你首先保证那个字段是唯一的，你要，你查询时候要带，要带上字段，你要增加速度，增加查询速度的时候，是吧？你就可以给它设置个唯一索引。

说话人 2 14:01 
了解。那比如说像我有三个字段，对吧？比如说A、B、C，对吧？我给A、 B 加联合索引，我的查询条件是A、B、C。然后这个联合索引会生效吗？

说话人 1 14:16 
加了 ABC 联合索引，给 AB 加了索引。

说话人 2 14:21 
对，对。

说话人 1 14:24 
然后查询带上这三个字段。

说话人 2 14:27 
嗯，对，是的。

说话人 1 14:28 
联合索引的话，查询带上这三个字段，前两个字段会生效，第三个的话，你是因为你没设置那个，第三个字段因为你没设置那个索引，没在联合索引之内，所以说不会生效，就前两个索引是生效。就你查 A 和 B 的时候。

说话人 2 14:55 
什么意思？那比如说我现在查ABC，那实际上的查询执行流程什么样子？

说话人 1 15:01 
啥？

说话人 2 15:02 
就是我现在，因为实际上查的是 ABC 嘛，它实际上的查询逻辑是什么？ AB 的， AB 建立联合索引，它会走吗？

说话人 1 15:12 
AB 建立联合索引会走，就是你那个查询条件是三个都 and 吗？都是用用 and 来连接的吗？

说话人 2 15:19 
嗯，对对对。

说话人 1 15:20 
三个 and 来连接的话，那你前两个是会走那个联合索引的，就是查那个，因为它符合那个联合索引那个规范，跟那个根据那个列来列的话，那你后面的话它，因为因为 C 没没索引嘛，没索引就只能走普通，就。正常查了，就没有索引，我的理解是这样的。

说话人 2 15:44 
了解。那比如说像 MySQL 里面的话，我们可以把一个用 UUID 去作为它的主键吗？

说话人 1 15:56 
用 UUID 的话，它是不稳定的吧，就它好像是无序的吧？

说话人 2 16:02 
对，无序的，假设 UUID 可以保证它的唯一。

说话人 1 16:06 
对，可以保证唯一，但是它是无序的，你你索，你用索引查就是利用它是有。保持构构建索引的话，用它来做主键索引吗？还是什么？

说话人 2 16:18 
唯一主键索引，对。

说话人 1 16:24 
主键索引是排序，你在，你在增加增。插入数据的时候不怎么好吧？因为它是无序的，你得保证这个数，这个索引数是有序的，所以说你插入的时候效率肯定是不如递增的这样子吧，递增的字段。嗯，还有其他吗？

说话人 2 16:46 
了解。还有其他吗？这个补充。

说话人 1 16:51 
补充，还有个 UUID 可能比较大呀，就那个比较大一点，那个占的内存大一点，因为数都比较大。应该我的理解是这样。

说话人 2 17:12 
了解。

说话人 2 17:27 
比如说我有一段程序，是要计算从 1 加到10，对吧？一种方式的话，就是我用一个数组去存了 1 到10。另外一种方式的话，是我用列表存了 1 到10，去循环累加。这两种程序有什么区别吗？

说话人 1 17:47 
有什么区别吗？

说话人 2 17:49 
对。

说话人 1 17:50 
一个数，一个数组存，一个链表存。

说话人 2 17:53 
对，是的。

说话人 1 17:55 
但做法都是进行累加到10。

说话人 2 17:58 
对，是的。

说话人 1 18:00 
结果好像没什么区别，数组和，你是想问数组跟链表的区别吗？还是什么？嗯，对。数组它是，它内存分布是连在一起的，可以这么理解，就可以直接通过偏移量连，链表的话，它是通过指针进行寻址。是它们最主要的，最最主要的区别，对吧？

说话人 2 18:24 
了解，性能呢？

说话人 1 18:27 
性能的话，它数组直接寻址更更快一点嘛，就直接通过一个偏移量，直接就能找，就能找到下个元素。更快一点吧，我的理解。

说话人 2 18:43 
可以说具体说一下它快到哪里吗？哪些地方它可能比较快？

说话人 1 18:49 
哪些地方？我能想到就是它内存紧凑，然后直接通过批量就能找到了，我的理解就只有只有这个。

说话人 2 19:04 
了解。

说话人 2 19:12 
可以说一下进程和线程的区别吗？

说话人 1 19:16 
进程和线程的区别，进程它是操作系统操作系统资源分配的单位了，单位，线程是操调度的基本单位。一个是资源分配了，一个是调度的基本单位。线程是蕴含在进程里面的，对。切换的话，进程切换的话需要保存上下，上上游比比较多，因为它比较比较大了。进程的话，切换的话成本相相对来说就较低一点。

说话人 2 20:00 
这是哪里低啊？就是这里可以具体说一下吗？

说话人 1 20:05 
哪里一点？线程是从进程里面分配出来的呀，可以可以这么理解吧，就是更。像你。画了更小的一个口子了呀，进程是操作系统资源分配最小单位了，你实际上动起来的话，是通过线程了，如果多有多线程的环境的话，那就是通过线程来进行动了，进行操作的，主要是调度的单位。

说话人 2 20:45 
了解，那比如说多进程和多线程的话，有什么区别？

说话人 1 20:49 
多进程和多线程的区别。

说话人 2 20:52 
就比如说有一个程序用多进程去实现和多线程实现有什么区别？

说话人 1 20:59 
他觉得现。进程和线程之间区别大，这不太清楚。

说话人 2 21:07 
了解。

说话人 2 21:14 
那进程通讯方式有哪些？了解吗？

说话人 1 21:18 
进程什么？

说话人 2 21:20 
进程通讯。

说话人 1 21:21 
通讯，进程通讯的话，我记得有共通过，有的是通过共享内存，好像共享一片内存区域来通信，有的是。这是一种管道通信吧，我所知道的。

说话人 2 22:02 
那比如说我有一段程序，还是刚才的信息累加求和。然后比如说从 1 加到1亿，对吧？然后一个呢是由一个线程去进行计算，另外一个呢是由两个线程去进行计算。一个线程去计算前半部分，另外一个线程去计算后半部分。他们是在运行在一个单核的服务器上面。你觉得这两种运行方式的话，它有什么差别？这个差别主要点在于哪里？

说话人 1 22:34 
一个是一个是单线程，一个是多线程嘛。

说话人 2 22:37 
嗯，对。

说话人 1 22:40 
多线程它是可以并行计算的吧，就一同同时开启之后，一个计算前半部分，一个另一个计算后半部分，可以并行的计算，但单线程它就只能从头到尾了。

说话人 2 23:02 
它是一单核服务器。它是一个单核服务器。

说话人 1 23:07 
单核啊，单核的话那，哦，我以为是 8 核，不好意思，我听错了。单核的话它不能够有那个。多线程吧，核数就已经限定线程的并发量了吧，那应该两者没什么太多的区别吧，我的理解。

说话人 2 23:33 
了解。

说话人 2 23:43 
可以说一下 HTPS 的工作原理吗？

说话人 1 23:46 
HTPS 它是我所知道它是个安全的连接，首先。首先那个客户端要向服务器，告诉服务器说要建立 HTTPS 连。对吧？服务器的话，它会通过， HTBS 它是有那个中间，有第三方人保证安全的。服务器的话，它会通过中间人，可靠的中间人生成一个数字数字签名，还生成一把公钥吧，发给客户端。客户端的话，就看，检查那个数字签名是否合法，因为那个签名保证，可以保证合法，如果检查通过的话，它就生成一个。

说话人 1 24:34 
生成个密钥吧，生成个密钥，通过服务器发过来的公钥来进行加密，加密之后再传给服务器，中间也有中间人的参与，服务器的话用自己的公钥解密，相当于就得到了客户端的密钥，两者就建立了 HTPS 的通信，相当于数据通，各自拿了一把密钥，这样子数据传出去。可靠了，有中间人的参与，可信第三方。

说话人 2 25:14 
那你刚才说中间人，那个中间人是干什么的？

说话人 1 25:18 
中间人就相当于一个客观的存在，保证安全的，可信的第三方嘛，来记，然后有类似一些签名什么的。保证，保证不被篡改，因为如果被篡改了，客户端或者服务器就能够知道这是假的，就不会建立通信了，他的数字签名能保证不被篡改。

说话人 2 25:48 
怎么保证不被篡改呢？

说话人 1 25:51 
怎么保证不被篡改的？

说话人 2 25:53 
对，数字签名又怎么生成的？

说话人 1 25:56 
通过一些算法吧，但我只知道是一通过一些算法，具体的具体的算法什么我不是很不清楚。

说话人 2 26:06 
好，了解。

说话人 2 26:14 
然后二。二叉树可以说一下吗？就是它的特性。

说话人 1 26:17 
二叉树的话就是它子节点，父节点，它子节点最多有两个了，二叉树特性，从根节点开始这样排列，特性差不多，它它的样子差不多是这样子，二叉树的定义就是一个子节点数不超过两个。

说话人 2 26:44 
了解，它它有什么问题吗？还有它的时间复杂度有多少？

说话人 1 26:51 
你是说查询是吗？对。查询的话它是 ON 的，因为它要遍历整个树嘛。所以说。后面就可能引用那些二叉排序树吧。

说话人 2 27:04 
二叉排序树是指。

说话人 1 27:05 
不，就那个，一个，后面的，二叉树一个，排序二叉树，用可以用来排序的数，排序二叉树，它就保证了，左节点比自己小，右节点比自己大，这样子排序，这样子是查找的效率就是 Ologn 了，这是二叉排序树。

说话人 2 27:32 
了解。平时的话， AI 有在了解过，了解些什么东西吗？

说话人 1 27:41 
就用过用过Codex，就主要用用过Codex，然后 Clash 也也用过，然后用来写代码什么的，还有一些问问题吧，就讨论问题差不多，就有一些不懂的也可以问问它， AI 举例子这样子，差不多。

说话人 2 28:01 
了解。 AI 相关的 agent 开发的相关的技术的话，有了解过哪些吗？

说话人 1 28:09 
我知道 AI 的话是用，主要是用 Python 吧。一些框架的话，兰倩好像我是看过了，但是只相当于过了一遍吧。兰倩我是看过了，对。就用一个构构建 AI 智能的框架了。

说话人 2 28:31 
了解，那你平时有在看一些 AI 哪方面的技术吗？

说话人 1 28:38 
哪方面的技术？对。就一些概念肯定要看看一些语言概念的话，比如说什么LLM、skill，这些，就因因 Codex 也也得用到这些，然后也得去了解这些。好的，还有一些智能体的一些运行过基本流程，也看过，就怎么运行的基本流程嘛。

说话人 2 29:08 
可以说一下吗？

说话人 1 29:10 
我所知道就是，这 A 建筑的话，它底层。好像我所知道一个是一个 React 模式，思考、行动、再观察，类似这样的一，这样一种方式，大模型先思考干嘛？行动的话，我们可以给大模型提供一些工具，什么东西调用，对吧？就定义一些函数，像这种作为工具可以，供它可以调用，它调用之后，产生结果，再把这个结果喂到那个消息里面，给它，它自己再反思，这样子，这个过程就又循环扩起来了，就直到达最终答案。差不多这个 reaction 模式的流程。

说话人 2 29:59 
了解，那有看。为什么需要 React 模式吗？

说话人 1 30:02 
为什么需要 React 模式？嗯，对。它是构建智能体的一个基本范式吧，智能体它就是能够，能达能实现这样，你通过这个 React 这个模式就实现了一个简单的一个智能体，它只能就是这样子了，要不然的话，大模型的话，我知道大模型它是没有那种记忆性的，你这没有记忆性的，我们要维持一些记忆，就维持一些消息给它，给它堆叠给它，这样它能根据前面的判断做出相应的结果。就 React 模式，然后构建一个简单智能体。

说话人 2 30:40 
了解。那你目前对 AI 目前是怎么理解？或者怎么看待的？

说话人 1 30:45 
怎么看待？现在肯定也得用用，也也肯定，你必不可少了，差不多。就你肯定也要跟AI，是，现在也是 AI 时代，也得用也得用AI，也得了解 AI 相关新闻什么的，对吧？ AI 后面也也会继续发展着，能力也会越来越强吧。

说话人 2 31:13 
了解，那你觉得 AI 对你来说最大挑战是什么呀？

说话人 1 31:16 
最大的挑战？

说话人 2 31:18 
对。

说话人 1 31:22 
怎样算最大？怎样算挑战呢？

说话人 2 31:27 
就是你觉得AI。到了以后，你觉得自己的价值是什么？换言之，就是说。

说话人 1 31:32 
自己的价值，自己价值感觉还好吧，就 AI 也能也能提效什么的，这也算，算是，有了 AI 之后，学习之，学习新东西也更快一点了，我觉得也有挑战，也有机遇吧，你学一个东西可以通过 AI 更快的学了，对吧？但是挑战的话就可能需要学的东西越来越多了，但是好处就是你学的东西，你学的东西越要越来越多，也算好处也算坏处吧。真的花时间学，但是学的东西也算自己的，对吧？ AI 的话就可以再帮忙提速。

说话人 2 32:17 
了解。我这里今天问的差不多，你这边有没有想要了解的？

说话人 1 32:26 
这边，公司那边是吧？

说话人 2 32:30 
对，就啥都行，我就看你这边有什么问题。

说话人 1 32:34 
我想问一下，公司那边现在做的业务是算什么？能问问一下吗？

说话人 2 32:41 
一块是 AI 智能体开发。另一块的话，就是一些之前那些传统的一些 web 应用， web 项目。

说话人 1 32:53 
工作时时时时间呢？想问一下。

说话人 2 32:59 
正常的话。是 9 点到 6 点。

说话人 1 33:02 
9 点到 6 点。

说话人 2 33:04 
对。

说话人 1 33:06 
然后一周时间是？

说话人 2 33:09 
5 天。

说话人 1 33:09 
哦，好的。然后还有什么？公司是没有包吃包住，是吧？

说话人 2 33:23 
对。

说话人 1 33:25 
应该好的，还有什么我想一下。

说话人 1 33:41 
公司这边入职之后的流程是怎么？是直接开始干活，还是先看一下文档什么的？是先熟悉一下流程。

说话人 2 33:50 
这个一般的话会有人带着你去做。

说话人 1 33:54 
类似于导导师这样带着我们做，是吧？

说话人 2 33:57 
对。

说话人 1 33:59 
好的好的，应该大概没有了。

说话人 2 34:06 
行，好的，那今天也感谢你去。来参加面试啊。

说话人 1 34:10 
嗯，好的。

说话人 2 34:11 
那就拜拜。

说话人 1 34:12 
好，拜拜，再见。

