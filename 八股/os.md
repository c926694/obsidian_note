# 进程、线程、协程

## 一句话定位

- **进程**：资源分配的最小单位
- **线程**：CPU 调度的最小单位
- **协程**：用户态调度的最小单位（轻量级线程）

## 三者对比总览

| 维度         | 进程                                 | 线程                             | 协程（goroutine）                          |
| ---------- | ---------------------------------- | ------------------------------ | --------------------------------------- |
| **本质/职责**  | 资源分配的最小单位                         | CPU 调度的最小单位                    | 用户态调度的最小单位                             |
| **调度者**    | 操作系统（内核态）                         | 操作系统（内核态）                      | 应用程序/语言运行时（用户态），如 Go Runtime            |
| **资源隔离**   | 独立虚拟地址空间、文件句柄、信号，隔离最强            | 共享进程的堆、代码段、文件描述符，隔离弱         | 共享线程资源，栈极小                              |
| **栈大小**    | 独立地址空间，无固定栈概念                     | 固定栈，通常 1MB                      | 初始 2-4KB，可动态伸缩                           |
| **创建成本**   | 大，需分配 PCB 和虚拟地址空间                 | 中，复用进程资源                       | 小，几 KB，几十万个没问题                          |
| **切换开销**   | 大，需换页表刷新 TLB                      | 小，仅保存寄存器、栈指针                  | 极小，~ns 级，完全用户态不陷内核                      |
| **通信方式**   | IPC（管道/共享内存/Socket），需内核介入         | 直接读写共享变量，无需内核但需加锁           | Channel（CSP 无锁）或共享内存 + 锁                 |
| **稳定性**    | 一个进程崩溃不影响其他进程，隔离性好               | 一条线程崩溃整个进程退出，一个挂全挂           | 一个 panic 不 recover 会终止进程，但可被 recover 捕获 |
| **数量上限**   | 几十到上百                             | 几千                              | 几十万                                     |

**一句话串起来**：进程是最重的隔离单位，线程是内核调度的执行单位，协程是在线程之上再抽象一层的用户态执行单位——用极低的切换成本在单线程上榨出高并发。

---

## 进程 vs 线程

| 维度          | 进程                                   | 线程                             |
| ----------- | ------------------------------------ | ------------------------------ |
| **资源分配**    | 独立内存空间、文件句柄、信号，资源隔离强                 | 共享进程的堆、代码段、文件描述符，资源开销小         |
| **创建与切换开销** | 创建需分配 PCB 和虚拟地址空间，切换需换页表刷新 TLB，开销大   | 创建直接复用进程资源，切换仅保存寄存器栈指针，开销小     |
| **通信**      | IPC（管道、共享内存、信号、Socket），需**内核**介入，效率低 | 直接读写共享变量，**无需内核**介入，效率高但需加锁防竞态 |
| **稳定性**     | 一个进程崩溃不影响其他进程，隔离性好                   | 一条线程崩溃整个进程退出，一个挂全挂             |

## 线程 vs 协程

线程是**操作系统**调度的基本单位，由内核管理，属于**内核态**调度；而协程是**用户态**的轻量级线程，由应用程序/语言运行时（Runtime）管理，属于**用户态**调度。

协程的本质是用更低的上下文切换开销，实现单线程上的高并发。

### goroutine vs OS 线程

| 维度       | goroutine（协程）                   | OS 线程                     |
| -------- | ------------------------------- | ------------------------- |
| **创建成本** | ~2KB 初始栈，轻量，几十万个没问题             | ~1MB 初始栈，重量，几千个就扛不住       |
| **调度**   | Go 运行时**用户态**调度（G/M/P），切换 ~ns 级 | 内核态调度，切换 ~μs 级（用户态↔内核态切换） |
| **栈大小**  | 初始小（2-4KB），可动态伸缩                | 固定栈大小（通常 1MB），浪费内存        |
| **创建销毁** | 快，`go func()` 就完事               | 慢，需要系统调用                  |
| **通信方式** | 可选：Channel（CSP，无锁）或共享内存+锁       | 只有共享内存+锁，天然需要锁            |

**核心**：goroutine 是用户态"轻量级线程"，由 Go 运行时自己调度，不依赖内核。完全在用户态完成切换，不需要陷入内核，所以快得多。

---

## 进程通信方式（IPC）

1. **管道（Pipe）**：包括匿名管道（父子进程间）和命名管道（FIFO，任意进程间）。
2. **消息队列（Message Queue）**：进程间通过消息队列传递结构化数据。
3. **共享内存（Shared Memory）**：一块内存区域映射到多个进程的地址空间，效率最高，但需要信号量同步。
4. **信号量（Semaphore）**：用于进程间同步，不传数据。
5. **信号（Signal）**：异步通知机制，如 SIGINT、SIGKILL。
6. **Socket**：可用于同一主机或不同主机的进程通信。

### 追问

**Q: 哪种 IPC 最快？**
共享内存。因为它直接把物理内存映射到进程地址空间，数据在内存里读写，不需要经过内核拷贝。其他方式（管道、消息队列、Socket）都需要内核做一次数据拷贝。

**Q: 共享内存有什么问题？**
多个进程同时写同一块内存会有竞态条件，所以必须配合信号量做同步。

**Q: 管道和消息队列的区别？**
管道是字节流无格式，读出来什么就是什么；消息队列有类型和格式，接收方可以按类型选择性地读。

**Q: Socket 和前面的有什么不同？**
只有 Socket 支持**跨网络**通信，其他都是同一台机器上的 IPC。HTTP、RPC、数据库连接底层都是 Socket。**Socket 和管道的本质**都是内核缓冲区 + 读写两端。**Socket 只是在内核缓冲区外面多套了一层网络协议栈**，所以能跨机器。而管道简单粗暴，连命名都没有（匿名管道只能父子进程用）。**所以说 Socket 就是管道的升级版，能跨网络的管道。**

## 单核服务器计算

一个单核服务器,一个线程算全部和两个线程各自算一半哪个快

单核纯计算 → **一个线程快**，省了切换开销。  
单核有 IO → **多线程快**，等待时让出 CPU 干别的。
