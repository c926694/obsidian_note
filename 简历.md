# [陈鹏] | 求职意向：agent开发 / 后端开发

**电话：** 13528593843 | **邮箱：** c926694@163.com 

---


## 项目经历

**视频 Feed 流系统**

**技术栈：Go / Gin / Gorm / MySQL / Kafka / gRPC / Etcd / Docker**

项目简介：基于 Go/Gin/GORM 构建短视频后端服务，覆盖**账号、视频发布与详情、点赞、评论、关注关系、Feed 流与热榜**等核心能力；采用 **JWT 鉴权与 token 失效控制**，引入 **自研分布式缓存集群** 与 **Kafka 事件驱动异步化**提升吞吐与稳定性，并通过 Docker Compose 实现一键部署与本地联调。

- 实现 **JWT + Redis 鉴权体系**，支持**用户登录鉴权**与 **token 状态校验**，通过 **Redis 维护 token 失效名单**实现登录态管理与异常 token 拦截。
- 设计**时间窗口化热榜算法**，支持**按时间窗口（如 60 分钟）动态计算热度**，提升热榜实时性与内容新鲜度。
- 构建**自研分布式缓存集群**，基于 **gRPC + 一致性哈希**实现多节点缓存分片，采用 **LRU 淘汰 + TTL 过期**策略缓存**热点视频详情、用户关系与交互状态**；通过 **singleflight 防击穿**机制合并并发缓存 miss 请求，降低 **MySQL 回源压力**；基于 **Etcd 注册发现**支持节点动态扩缩容，本地压测 **QPS 达 5.3w**。
- 基于 **Kafka 构建事件驱动异步处理**机制，将**点赞、评论更新等操作异步化**，解耦主链路并降低接口延迟。

---


EchoMind 智能客服系统

**技术栈：Python / FastAPI / Anthropic Claude  / Redis / ChromaDB / Docker** 

项目简介：基于 FastAPI + Anthropic SDK 构建智能客服系统，核心能力覆盖**多 Agent 编排**（意图路由 + 性能路由 + 降级）、**三级记忆架构**（Redis 工作记忆 + ChromaDB 情景记忆/用户画像）、**RAG 知识库检索**（查询改写 + 重排）、**MCP 工具调用**（熔断/缓存/降级）、**Monitor 自动降权闭环**、**LLM-as-Judge 评测**等完整智能客服能力。

- 设计并实现**三路融合意图识别**模块，结合 **LLM 语义理解**、**Embedding 相似度**和**关键词 Pattern**，
  通过**加权投票**输出意图、置信度、紧急度和结构化实体；兼容第三方 API 时自动禁用 Embedding 分支。
- 实现 **Tool 工具调用框架**，封装参数校验、**TTL 缓存**、**超时控制**、**熔断器**和 **fallback**，
  并通过查询改写、并行召回和 **LLM 重排**优化 **RAG 知识库检索**质量。
- 将**RAG 知识库检索**接入 `/chat` 主链路，在 Agent 回复前**自动检索 ChromaDB `knowledge_base`**
  并注入 **Top-K 知识片段**，降低模型幻觉并提升业务规则回答准确性。
- 实现**动态 Skills 加载**能力，支持从 **Markdown/JSON 文件**加载通用客服、技术支持、账单退款规范。
- 设计 **Redis + ChromaDB 三级记忆架构**，使用 **Redis** 管理**当前会话工作记忆**，
  使用 **ChromaDB** 存储**历史对话摘要**和**用户画像**，支持**自动压缩**和**跨会话语义检索**。
- 实现**多 Agent 编排**系统，支持**意图路由**、**性能路由**和**降级路由**；
  对同时涉及技术和账单的**复合问题**自动并行派发给**多个 Agent 并合并结果**。
- 构建 **Monitor 在线监控闭环**，定时采集 **Agent/工具成功率**和**延迟**，
  通过 **monitor_penalty** 写回 **Orchestrator**，动态降低问题 Agent 的 **routing_score**。
- 实现**端到端评测框架**，真实调用 **Orchestrator** 生成回复，并使用 **LLM-as-Judge**
  从相关性、准确性、完整性和有用性四个维度评分，支持回归检测和优化建议生成。

---

## 专业技能

**语言基础：** 熟悉 Go、python 语言，深入理解 slice、map、channel 原理，了解 GMP 模型及协程调度、GC 垃圾回收机制。

**web框架**: 熟悉gin、gorm、fastapi、SQLAlchemy

**中间件：** 熟悉 MySQL、Redis、kafka

**计算机基础：** 掌握计算机网络（TCP/HTTP/HTTPS）、操作系统（进程/线程、内存、I/O）相关知识。

**工程能力：** 熟悉 Docker、Linux、Git 常用操作。

**算法与数据结构：** 熟悉常用数据结构（栈、队列、树、图等）。


## 教育背景

**湖南城市学院** &emsp;&emsp; 计算机科学与技术（本科） &emsp;&emsp; 2024.09–2028.06

- **荣誉奖项：** 第十七届蓝桥杯省级 Java 竞赛获奖

---
